# ArcJSON-Style
Tool to take basic inputs from user to create ArcJSON styling intended for uGRIDD uSHOW tools.

Currently supports singular style for point, line, polygon. In the future, this tool should provide option for multiple styles within the same dataset.
